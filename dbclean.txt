TRUNCATE `Activity_activity`;
TRUNCATE `Activity_chatter`;
TRUNCATE `Activity_maps`;
TRUNCATE `Attachment_attachment`;
TRUNCATE `BusinessPartner_bpaddresses`;
TRUNCATE `BusinessPartner_bpbranch`;
TRUNCATE `BusinessPartner_bpemployee`;
TRUNCATE `BusinessPartner_businesspartner`;
TRUNCATE `BusinessPartner_businesspartner_BPLID`;
TRUNCATE `Caller_caller`;
TRUNCATE `Campaign_campaign`;
TRUNCATE `Campaign_campaignset`;
TRUNCATE `Campaign_campaignsetmembers`;
TRUNCATE `Employee_target`;
TRUNCATE `Employee_targetqty`;
TRUNCATE `Employee_targetyr`;
TRUNCATE `Expense_expense`;
TRUNCATE `Invoice_addressextension`;
TRUNCATE `Invoice_documentlines`;
TRUNCATE `Invoice_invoice`;
TRUNCATE `Lead_chatter`;
TRUNCATE `Lead_lead`;
TRUNCATE `Lead_leaditem`;
TRUNCATE `Notification_notification`;
TRUNCATE `Opportunity_line`;
TRUNCATE `Opportunity_oppitem`;
TRUNCATE `Opportunity_opportunity`;
TRUNCATE `Opportunity_stage`;
TRUNCATE `Order_addendumrequest`;
TRUNCATE `Order_addressextension`;
TRUNCATE `Order_documentlines`;
TRUNCATE `Order_order`;
TRUNCATE `Project_project`;
TRUNCATE `Quotation_addressextension`;
TRUNCATE `Quotation_documentlines`;
TRUNCATE `Quotation_quotation`;
TRUNCATE `Tender_corrigendumlist`;
TRUNCATE `Tender_coverdetail`;
TRUNCATE `Tender_critcaldates`;
TRUNCATE `Tender_documents`;
TRUNCATE `Tender_lowestone`;
TRUNCATE `Tender_paymentinstrument`;
TRUNCATE `Tender_technicalopening`;
TRUNCATE `Tender_tender`;
TRUNCATE `Tender_tenderopening`;
TRUNCATE `Tender_tendersubmission`;
TRUNCATE `Tender_tenitem`;